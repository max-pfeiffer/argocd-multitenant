# PLACEHOLDER — this file is generated, not authored.
#
# It must contain the SealedSecret objects produced by the `kubeseal` step in
# infra/pki/step-ca-secrets.md, sealed with THIS cluster's public key for the `step-ca` namespace.
# Sealed data is cluster-specific: it cannot be written ahead of time or copied between clusters.
#
# Until it is generated, `infra/apps/step-certificates.yaml` (wave 3) will fail to sync, which is
# the intended behaviour — the alternative is a chart-generated CA (guardrail 30).
